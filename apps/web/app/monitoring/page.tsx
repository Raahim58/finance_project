"use client";
import { useEffect,useMemo,useState } from "react";import { Icon } from "@/components/Icon";import { Portfolio,acknowledgeAlert,getAlerts,getPortfolios } from "@/lib/api";
const val=(r:Record<string,unknown>,...keys:string[])=>{for(const k of keys)if(r[k]!=null)return String(r[k]);return""};
export default function MonitoringPage(){const [rows,setRows]=useState<Array<Record<string,unknown>>>([]);const [portfolios,setPortfolios]=useState<Portfolio[]>([]);const [portfolio,setPortfolio]=useState("");const [loading,setLoading]=useState(true);const [error,setError]=useState("");useEffect(()=>{void getPortfolios().then(setPortfolios).catch(()=>{})},[]);useEffect(()=>{setLoading(true);void getAlerts(portfolio||undefined).then(setRows).catch((e:Error)=>setError(e.message)).finally(()=>setLoading(false))},[portfolio]);async function acknowledge(id:string){try{await acknowledgeAlert(id);setRows(r=>r.filter(x=>String(x.id)!==id))}catch(e){setError(e instanceof Error?e.message:"Could not acknowledge alert")}}const groups=useMemo(()=>Object.groupBy(rows,r=>val(r,"severity")||"info"),[rows]);const order=["critical","high","medium","warning","low","info"];return <div className="page-wrap"><header className="page-heading"><div><p className="eyebrow">Exception management</p><h1 className="page-title">Monitoring</h1><p className="page-subtitle">Only active exceptions, freshness issues and mandate breaches that require attention.</p></div><select aria-label="Portfolio filter" className="field w-full sm:w-64" value={portfolio} onChange={e=>setPortfolio(e.target.value)}><option value="">All portfolios</option>{portfolios.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></header><div className="metric-strip mb-3 grid-cols-4"><Metric label="Critical" value={String((groups.critical??[]).length)}/><Metric label="High" value={String((groups.high??[]).length)}/><Metric label="Warnings" value={String((groups.medium??[]).length+(groups.warning??[]).length)}/><Metric label="Total open" value={String(rows.length)}/></div>{error?<div className="notice notice-error"><Icon name="warning"/>{error}</div>:null}{loading?<div className="panel h-72 skeleton"/>:rows.length?<div className="grid gap-3">{order.filter(k=>groups[k]?.length).map(severity=><section className="panel" key={severity}><div className="panel-head"><h2 className="panel-title capitalize">{severity} severity</h2><span className={`badge ${severity==="critical"||severity==="high"?"badge-bad":"badge-warn"}`}>{groups[severity]?.length} open</span></div><div className="divider-list">{groups[severity]?.map((r,i)=>{const id=String(r.id??i);return <article key={id} className="grid gap-2 p-3 md:grid-cols-[160px_1fr_auto]"><div><p className="text-[10px] font-bold uppercase tracking-wide text-muted">{val(r,"rule_type","category","alert_type")||"Portfolio exception"}</p><p className="data-font mt-1 text-[10px] text-muted">{val(r,"created_at","triggered_at")||"Time unavailable"}</p></div><div><p className="text-xs font-semibold">{val(r,"message","title")||"Alert details unavailable"}</p><p className="mt-1 text-[11px] text-muted">{val(r,"evidence_summary","reason")||"No additional evidence summary was returned."}</p></div><button className="btn btn-secondary self-center" onClick={()=>acknowledge(id)}>Acknowledge</button></article>})}</div></section>)}</div>:<div className="panel empty-state"><Icon name="check" size={28}/><strong>No active exceptions</strong><span>Monitoring has not returned an alert for the selected scope.</span></div>}</div>}
function Metric({label,value}:{label:string;value:string}){return <div className="metric"><p className="metric-label">{label}</p><p className="metric-value data-font">{value}</p></div>}
