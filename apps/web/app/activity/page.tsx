"use client";
import { useEffect,useState } from "react";import { Icon } from "@/components/Icon";import { Portfolio,Transaction,getPortfolios,getTransactions } from "@/lib/api";
const money=(v:unknown)=>`PKR ${new Intl.NumberFormat("en-PK",{maximumFractionDigits:0}).format(Number(v||0))}`;const title=(v:string)=>v.replaceAll("_"," ").replace(/\b\w/g,c=>c.toUpperCase());
export default function ActivityPage(){const [portfolios,setPortfolios]=useState<Portfolio[]>([]);const [selected,setSelected]=useState("");const [rows,setRows]=useState<Transaction[]>([]);const [filter,setFilter]=useState("all");const [loading,setLoading]=useState(true);useEffect(()=>{void getPortfolios().then(p=>{setPortfolios(p);setSelected(p.find(x=>x.is_default)?.id??p[0]?.id??"")}).finally(()=>setLoading(false))},[]);useEffect(()=>{if(selected)void getTransactions(selected).then(setRows).catch(()=>setRows([]));else setRows([])},[selected]);const visible=rows.filter(r=>filter==="all"||r.transaction_type===filter);return <div className="page-wrap"><header className="page-heading"><div><p className="eyebrow">Audit trail</p><h1 className="page-title">Activity</h1><p className="page-subtitle">A chronological transaction ledger sourced from stored portfolio records.</p></div><div className="flex w-full gap-2 sm:w-auto"><select aria-label="Portfolio" className="field" value={selected} onChange={e=>setSelected(e.target.value)}>{portfolios.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select><select aria-label="Activity type" className="field" value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">All types</option>{Array.from(new Set(rows.map(r=>r.transaction_type))).map(t=><option value={t} key={t}>{title(t)}</option>)}</select></div></header>{loading?<div className="panel h-72 skeleton"/>:<section className="panel"><div className="panel-head"><h2 className="panel-title">Transaction ledger</h2><span className="text-[10px] text-muted">{visible.length} records</span></div>{visible.length?<div className="table-wrap"><table className="data-table"><thead><tr><th>Date</th><th>Type</th><th>Security</th><th>Quantity</th><th>Price</th><th>Gross amount</th><th>Fees & tax</th><th>Source</th></tr></thead><tbody>{visible.map(r=><tr key={r.id}><td>{r.transaction_date}</td><td><span className="badge">{title(r.transaction_type)}</span></td><td className="font-bold">{r.symbol}</td><td className="data-font">{r.quantity??"—"}</td><td className="data-font">{r.price?money(r.price):"—"}</td><td className="data-font">{money(r.amount)}</td><td className="data-font">{money(Number(r.fees)+Number(r.taxes))}</td><td>{r.source}</td></tr>)}</tbody></table></div>:<div className="empty-state"><Icon name="activity" size={28}/><strong>No transactions</strong><span>Ledger entries for the selected portfolio will appear here.</span></div>}</section>}</div>}
